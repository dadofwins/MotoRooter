"""A one-line characterisation of a trip, for the chat rail's header.

Deliberately not part of `chat`. A rider who builds a whole trip with the mouse gets a blurb
too — if this only had something to say when there was chat history, the feature would become
a reward for typing, and "chat is an accelerator, never a requirement" is the rule that
forbids it. The trip document is the input; history is extra context when it happens to
exist.
"""
